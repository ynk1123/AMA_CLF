# TODO

- [ ] Update `portal-frontend/src/pages/Admin.js`:
  - [ ] Mobile-only: convert top stat cards into a 2x2 grid (no desktop changes).
  - [ ] Mobile-only: replace each table (Items, Pending Claims + All Claims, Users, Appointments) with card layout where each row becomes a card.
  - [ ] Ensure accessibility: min touch targets 44x44 for buttons/icon buttons, scalable typography, and vertical padding on mobile.
  - [ ] Keep desktop/tablet tables unchanged behind `md` breakpoints.
- [ ] Sanity check: run frontend lint/build/tests if available.

